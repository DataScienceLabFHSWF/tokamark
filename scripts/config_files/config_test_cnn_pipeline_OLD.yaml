max_index: 2
shuffle: true
seed: 42
local: false

cnn_settings:
  D: 64

dataloader_settings:
  # batch_size: 256
  # num_workers: 32
  batch_size: 32
  num_workers: 4
  # batch_size: 8
  # num_workers: 0

training_args:
  lr: 0.00001
  max_epochs: 500
  patience: 5

paths:
  data_output_directory: /output_cnn_test_demo/
