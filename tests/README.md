# Tests

This directory contains smoke tests for some functionality in the code base. To run the tests, run

```bash
python run_tests.py
```

This will execute every `.py` file in the `tests/` directory (this one), excluding `run_tests.py` from recursion.

To add a test to the suite, add a `.py` file to the `tests/` directory.