******************* TO DO *******************
$$ MAST_tools/plotting_utils.py review $$

    1- Import statement consistency:

        try:
            from . import store_utils
            from . import signal_utils
            from . import constants as cc
        except ImportError:
            import store_utils
            import signal_utils
            import constants as cc

            The exception is issued also for internal import failures in the modules store_utils, signal_utils and
            constants. Is this the intended use?

        From ROH:
        - This is now solved via the pip installation pipeline for the repo, which lets those imports (and similar) be
          written as:

          from MAST_tools import store_utils
          from MAST_tools import signal_utils
          from MAST_tools import constants as cc

          As a result, internal import failures are not mistakenly skipped anymore.


    2- Remove big chunk of commented code aka plot_cameras starting at line 247

        From ROH:
        - Pending to be checked. If code snippet still does not work after check, it will be removed.


    3- Recurring TypeError:
        TypeError: typing.Optional requires a single type. Got (<class 'numpy.ndarray'>, <class 'matplotlib.axes._axes.Axes'>).

        This error repeats consistently throughout the code.

        From ROH:
        - It did not raise any warnings for me, but anyway I fixed it.
        - The solution boils down to replacing `Optional[<a>, <b>]` with `Optional[Union[<a>, <b>]]`.


$$ store_utils.py review $$
    1- Method get_all_signals fails

        line 829
        if local:
            root_signals_path = os_join(base_parquet_path, test_and_level_case, "all_signals")

        ../../metadata/parquet/level2/all_signals does not exist

        From ROH:
        - It fails for `via_parquet = True` if parquet metadata has not been downloaded yet. It now raises an exception
          with a message describing how to download the required metadata.


    2- ? in path, is it a typo?
        full_path = f"{base_parquet_path}/{test_and_level_case}/signals?shot_id={shot_id_ii}"
        The ? character is invalid in Windows file and directory names, consider changing it to a more portable one.

        From ROH:
        - This path does not concern a Windows path but a URL, and "?" defines the start of the querying string.
        - So, `full_path` has been renamed as `target_url` to reflect its nature.


******************* ALREADY IMPLEMENTED *******************

$$ MAST_dataset.py review $$

    1- Change variable name.

        Line 257 and 262 define the variable list_chunks.
        This name is reminiscent of the shot-transform operation carried out by the window_segmenter_transform.py which
        indeed cuts signals into chunks. I suggest changing the name to a more general one which is agnostic of the
        transforms included in the shot-transform map. Also, we do not know whether the return values in 257 and 262
        are actually lists, therefore the name list_chunks should be avoided.

        New variable name: item.

        From ROH: OK.


    2- Add Cache option

        class CachedDataset(Dataset):
            def __init__(self, base_dataset):
                """
                base_dataset: torch Dataset
                """
                self.base_dataset = base_dataset
                self.cache = [None] * len(base_dataset)
                self._is_cached = [False] * len(base_dataset)

            def __getitem__(self, idx):
                # If not cached, load it once
                if not self._is_cached[idx]:
                    item = self.base_dataset[idx]

                    self.cache[idx] = item
                    self._is_cached[idx] = True

                return self.cache[idx]

            def __len__(self):
                return len(self.base_dataset)

        From ROH:
        - This was introduced in the wrong place. I moved it to a better place, and added the missing docstrings.


$$ scripts/utils.py review $$
    1- This file should be moved in fairmast-data-preprocessing/scripts/benchmarking/data_processing since defines
       methods that are exclusively used in the benchmarking folder.

       From ROH: OK.


$$ pipelines/utils/utils.py $$
    1- The method get_train_test_val_shots is not robust and I improved it.
       The new method preserves the behaviour of the former, in case only max_index_for_* are not given, otherwise it
       will crop the signals accordingly. In this way, we have different configurable limits for the three
       kinds of sets: test, validation and training. Plus, I added the option to shuffle them.

       From ROH:
       - No longer part of this repository.


$$ scripts/MAST_tools/store_utils.py $$
    1- General comment about concatenating paths:
        os.path.join is more robust than the f" " base approached used throughout the code:
        For instance, I recommend using:
        os.path.join(base_parquet_path, test_and_level_case, "all_signals")

        From ROH:
        - Replacing the original approach with `os.path.join` broke the code for Windows users as the remote paths in
          remote mode must conform with Linux-like paths.
        - I introduced a workaround via by replacing `os.path.join` with `posixpath.join` for remote pipelines and in
          other problematic places, which was successfully tested.


******************* SUGGESTIONS *******************
$$ MAST_dataset review $$
    1- Change return type.

        I do not see the need to cast self.shot_level_transform(shot) into a list. For instance, why are we using:
        return list_chunks if isinstance(list_chunks, list) else [list_chunks]
        I would suggest to return list_chunks directly (with a different name as in point 1.)

        From ROH:
        - No need to change the return type, as `__getitem__` is expected to return list, but `shot_level_transform` is
          user-defined. Thus, the casting is needed in case `shot_level_transform` does not return list.


    2- Remove methods

        Are the methods get_shot_id and get_windows_for_shot really needed?

        They are only used in the test function in the MAST_dataset. In addition, they do not do much. For instance,
        the get_shot_id is simply an alter-ego for self.shots_list[idx].  It seems an overkill to have a function to
        run only that line.

        From ROH:
        - While `shots_list[idx]` can be seen as an overkill, it is not obvious that shot ID can be obtained from
          `shots_list` for a given shot index. Additionally, `get_windows_for_shot` is not an overkill. Let's check
          this with Tobia, as he introduced these two methods.


$$ scripts/MAST_tools/store_utils.py $$
    1- General comment about code at 579, 711, 819:
        if base_parquet_path is None:
            raise ValueError(f"Invalid value '{base_parquet_path}' for attribute 'base_local_parquet_path' of the "
                             f"MASTStorageManager instance.")
        I wonder in what case `base_parquet_path` is None, since its default value is not None and nowhere in the
        store_utils base_parquet_path is re-initialized.

        From ROH:
        - The case for `base_parquet_path` being None is when the user creates an instance of `MASTStorageManager` with
          `base_parquet_path = None`.
        - Thus, the check is valid and must remain.


    2- Relative path
        The test works only when is run from the local directory of the script.

        From ROH:
        - This is now solved via the pip installation pipeline for the repo and the use of absolute paths.
